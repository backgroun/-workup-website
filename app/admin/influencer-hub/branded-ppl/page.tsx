import IHPagePlaceholder from "@/components/ih/IHPagePlaceholder";

export default function IHBrandedPplPage() {
  return (
    <IHPagePlaceholder
      breadcrumbLabel="브랜디드 / PPL"
      title="브랜디드 / PPL"
      description="모델 DB · 채널/인플루언서 DB — 게런티·단가는 권한이 있는 사용자에게만 표시됩니다"
      emptyMessage="등록된 모델/채널이 없습니다"
      emptyHint="등록 후 이곳에 목록이 표시됩니다"
    />
  );
}
